# Frontend Lib

Framework-level utilities and singleton helpers live here.

Examples: API client factory, TanStack Query client configuration, environment variable validation, typed route constants.

`lib/` must stay domain-neutral. It must not import feature modules or contain product business logic.
