name: Frontend Quality

on:
  # Runs on PRs targeting the integration branch (develop) and prod (main), and on push to
  # those branches. Path filters keep it scoped to frontend changes to limit Actions minutes;
  # use workflow_dispatch to run on demand.
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend.yml'
  push:
    branches:
      - main
      - develop
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: frontend-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: '10'

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'pnpm'
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # warn-only for now: pre-existing high advisories (e.g. Next.js < 16.2.5) are
      # being cleared via Dependabot PRs. Flip to a hard gate (drop continue-on-error)
      # once the backlog is green - story 0.7 AC3.
      - name: Audit (high+ severity)
        run: pnpm audit --audit-level high
        continue-on-error: true

      - name: Lint
        run: pnpm lint

      - name: Type-check
        run: pnpm typecheck

      - name: Test if available
        run: pnpm run --if-present test
        env:
          NEXT_PUBLIC_TWITCH_CHANNEL_LOGIN: test-channel

      - name: Build
        run: pnpm build
        env:
          NEXT_PUBLIC_TWITCH_CHANNEL_LOGIN: test-channel

      - name: Bundle analysis
        run: ANALYZE=true pnpm exec next build --webpack
        env:
          NEXT_PUBLIC_TWITCH_CHANNEL_LOGIN: test-channel

      - name: Upload bundle analysis
        uses: actions/upload-artifact@v7
        with:
          name: bundle-analysis
          path: frontend/.next/analyze/
          retention-days: 30
